# Directory to store install files

Place your install file from RBC into this directory.  You should have
only one install file.  The script loops thru all zip files and
uses the last one it finds.